# Emacs plugin jedi creates the directory.
.emacs.d/.python-environments

# chinese-pyim personal dict keeps changing - noisy.
.emacs.d/pyim/pyim-personal.txt

# The file is ~20M, pull separately.
.emacs.d/pyim/dicts/pyim-bigdict.pyim
