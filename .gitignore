# ==============================================================================
# Emacs: auto-managed (runtime-generated, not version-controlled)
# ==============================================================================
emacs.d/auto-managed/
emacs.d/.lsp-session-v1
emacs.d/.python-environments

# ==============================================================================
# Emacs: packages and parsers (downloaded, not version-controlled)
# ==============================================================================
emacs.d/elpa/
emacs.d/tree-sitter/
# pyim: personal dict changes frequently; bigdict is ~20M, pulled separately.
emacs.d/pyim/

# ==============================================================================
# Shell
# ==============================================================================
# Host-specific zsh overrides, not tracked.
zshrc.d/local.env.zsh

# ==============================================================================
# Desktop / OS
# ==============================================================================
# GTK bookmarks are path-dependent.
config/linux/gtk-3.0/bookmarks
